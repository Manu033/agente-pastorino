DB_HOST=localhost
DB_PORT=5432
DB_NAME=tu_base
DB_USER=solo_lectura
DB_PASSWORD=tu_password

OPENAI_API_KEY=tu_api_key

ALLOWED_TABLES=facturas,clientes,pagos
MAX_ROWS=100
SCHEMA_DESCRIPTION=Tablas disponibles: facturas(id, cliente_id, fecha_emision, fecha_vencimiento, importe, estado); clientes(id, nombre, cuit); pagos(id, factura_id, fecha_pago, importe). Los estados de factura habituales son pagada e impaga.